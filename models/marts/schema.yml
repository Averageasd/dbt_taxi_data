version: 2

models:
  - name: dim_vendor
    description: "This table contains vendor info"
    columns:
      - name: vendor_id
        description: "The unique identifier for each vendor."
      - name: vendor_name
        description: "The name of the vendor."
